# Аудит экономической логики проекта v0.10.15

**Назначение документа:** независимая проверка формул, коэффициентов и
логики расчёта экономической выгодности проекта. Все цифры безразмерные
(условные единицы, «у.е.»), не рубли. Базовая единица —
**1.0 у.е. ≈ себестоимость м² жилья 9-этажного монолита со
standard-отделкой**.

**Охват документа:** только экономический блок (`src/urban_model/economy/`):
себестоимость → выручка → метрики. Расчёт самих ТЭП (площади, население,
парковки, КИТ) — отдельный слой и в этот аудит не входит; экономика
принимает готовый `TEPResult` на вход.

---

## Результаты независимого аудита (v0.10.17)

Документ прошёл внешнюю проверку. Итоги:

| # | Замечание | Статус |
|---|---|---|
| 1 | **Двойной учёт встроенного ДОО** — здание входило в `cost.residential` (×1.00) и в `cost.kindergarten` (×1.40) одновременно | ✅ **ИСПРАВЛЕНО** в коде: `cost.py` вычитает площадь здания встроенного ДОО из жилой GFA (как ВПП) |
| 2 | **Себестоимость не зависела от класса** → бизнес выглядел чрезмерно прибыльным (ROI ~49%) | ✅ **ИСПРАВЛЕНО**: введён `residential_class_factor` 0.88/1.00/1.15; бизнес-ROI стал ~37% |
| 2b | **Цена эконом-класса 1.55 занижена** (стресс-вариант как дефолт) | ✅ economy 1.55→1.70 (v0.10.20). Эконом стал слабо прибыльным (+5 599), а не убыточным |
| 3 | Цены квартир business=2.80 | ✅ оставлены базовыми; завышенная прибыль снята п.2 |
| 4 | §3.2: формула парковок без `× sale_rate` | ✅ **ИСПРАВЛЕНО в доке** (код был верен) |
| 5 | §6.4: знак эффекта `only_demand` показан неверно | ✅ **ИСПРАВЛЕНО в доке** |
| 6 | Соц-компенсация 0.7 подана как «реальность» | ⚠️ **документировано как допущение сценария выкупа** (см. §3.5); UI-выбор режима — в планах |
| 7 | Overhead «≈26%» → фактически **28.4%** | ✅ **ИСПРАВЛЕНО в доке** (арифметика) |

### Коэффициенты по достоверности

| Группа | Коэффициенты |
|---|---|
| **Откалибровано под рынок СПб 2024** | себестоимость парковок (0.05 / 0.30 / 0.40), цены продажи м/м (3.0 / 10.0 / 18.0), доли реализации м/м по классу (0.20 / 0.45 / 0.75) |
| **Принято условно (оценка)** | себестоимость жилья по этажности (0.92…1.48), класс-фактор (0.88/1.00/1.15), ВПП (0.78), ДОО (1.40), СОШ (1.30), цены квартир по классу (1.70/1.95/2.80), overhead (10/5/5/7 %), прогрессия подземки, соц-компенсация 0.7 |

**Остаётся допущением (не баг, осознанный выбор модели):**
- `fixed = 0` (§2.9) — земля / ТУ / снос не моделируются. Итог — это
  `profit_before_land` («прибыль до стоимости земли и финансирования»),
  а не полная прибыль девелопера.
- Соц-компенсация 0.7 — сценарий частичного выкупа объектов городом;
  без неё почти любой квартал убыточен. См. §3.5.

---

## Содержание

1. [Базовая единица и философия модели](#1-базовая-единица)
2. [Себестоимость (Cost)](#2-себестоимость)
3. [Выручка (Revenue)](#3-выручка)
4. [Метрики (Profit / Margin / ROI)](#4-метрики)
5. [Пример полного расчёта (3 класса)](#5-пример-полного-расчёта)
6. [Где проходит граница рентабельности](#6-граница-рентабельности)
7. [Чувствительность к параметрам](#7-чувствительность)

---

## 1. Базовая единица

Все стоимости и цены в модели — **безразмерные «баллы выгодности»**.
**1.0 балл = условная себестоимость м² жилой GFA 9-этажного монолита со
standard-отделкой**. Это сделано, чтобы:

- Уйти от привязки к курсу рубля и инфляции.
- Позволить сравнивать проекты в относительных терминах.
- Упростить калибровку — все коэффициенты — доли от референса.

Все коэффициенты в `configs/spb.yaml` секция `economy:` отвечают этому
правилу.

---

## 2. Себестоимость

### 2.1 Жильё

```
cost_residential = (gfa − bi_area − built_in_kg_area) × C_base(floors) × class_factor
```

где:
- `gfa` — общая площадь жилых зданий, м² (из `TEPResult.gfa.value`);
- `bi_area` — суммарная площадь ВПП внутри жилого дома, м²
  (вычитается из GFA — ВПП учитывается отдельно);
- `built_in_kg_area` — площадь здания **встроенного** ДОО (если есть и не
  «только потребность»). **Аудит #1:** вычитается из жилой GFA, иначе
  оболочка ДОО считалась бы дважды — как жильё (×C_base) и как ДОО (×1.40).
  Для отдельно стоящего ДОО = 0 (его здание не входит в `gfa`);
- `C_base(floors)` — коэффициент себестоимости м² GFA от этажности,
  piecewise из `economy.construction.residential_by_floors`:

| Этажность | Коэффициент | Что значит |
|---|---|---|
| ≤ 5 | 0.92 | низкая монолит/кирпич, дешевле |
| ≤ 9 | **1.00** | референс (9-эт. монолит standard) |
| ≤ 12 | 1.10 | требуются лифты, усиленный фундамент |
| ≤ 17 | 1.20 | средне-высотная |
| ≤ 22 | 1.35 | башня, усиленный конструктив |
| > 22 | 1.48 | сложная инженерия |

- `class_factor` — **повышающий коэффициент по классу** (аудит #2),
  `economy.construction.residential_class_factor`:

| Класс | Фактор | Почему |
|---|---|---|
| economy | 0.88 | проще фасады/отделка/инженерия |
| comfort | **1.00** | референс |
| business | 1.15 | дороже фасады, МОП, высота этажей, отделка |

**Источник:** SPEC.md + оценка по практике СПб.

### 2.2 ВПП (встроенно-пристроенные помещения)

```
cost_vpp = bi_area × 0.78
```

где `0.78` — `economy.construction.vpp_commercial`. Shell-and-core
коммерческой ВПП без отделки арендатора (обычно строится застройщиком,
далее арендатор сам отделывает под бренд).

### 2.3 ДОО / СОШ

```
cost_kg  = kg_building_area  × 1.40
cost_sch = sch_building_area × 1.30
```

`kg_building_area` и `sch_building_area` рассчитываются в
`calculations/kindergarten.py` и `school.py` через piecewise норматив
`building_area_per_place` × вместимость объектов.

**v0.9.14 (ВАЖНО):** при `kindergarten.only_demand=True` (объект
размещается ВНЕ квартала) себестоимость **обнуляется**:
```python
if options.kindergarten.only_demand:
    cost_kg = 0.0
```
Логика: застройщик не строит объект — нет себестоимости. Аналогично
для СОШ.

Коэффициенты 1.40 / 1.30 учитывают спецтребования к соцобъектам
(вентиляция, безопасность, отделка).

### 2.4 Парковки

#### Открытые наземные
```
cost_parking_open = n_open × 20.75 × 0.05
                  = n_open × 1.04
```
- `n_open` — число открытых м/м
- `20.75` — м²/м.м (из `parking.open_space_per_place`, СПб)
- `0.05` — себестоимость м² (асфальт + разметка, ≈ 10 тыс ₽/м²)

#### Многоуровневые наземные
```
cost_parking_ml = n_ml × 28 × 0.30
                = n_ml × 8.40
```
- `28` м²/м.м (из `economy.parking_areas.multilevel_m2_per_space`)
- `0.30` — себестоимость (монолит + перекрытия, ≈ 60 тыс ₽/м²)

#### Подземные (с прогрессией по уровням)

```
Для подземки на N уровней, общее число мест распределено равномерно:
  places_per_level = n_ug / N
  area_per_level = places_per_level × 35

Себестоимость = Σ(level=1..N): area_per_level × 0.40 × progression(level)
```

`progression(level)`:

| Уровень | Множитель |
|---|---|
| 1 | 1.00 |
| 2 | 1.30 |
| 3 | 1.65 |
| 4 | 2.05 |
| 5 | 2.50 |
| 6+ | 3.00 |

Каждый уровень глубже — дороже из-за земляных работ, гидроизоляции,
дренажа, рамп. Источник: типовая прогрессия подрядчиков СПб.

#### Парковки соцобъектов
```
cost_social_parking = social_parking_area × 0.05
```
То же что открытые наземные — это м/м на ЗУ ДОО/СОШ.

### 2.5 Спортивные сооружения

```
cost_sport = sport_area × 0.05
```
Та же ставка что покрытие открытых парковок — это benthouse площадки,
покрытие, оборудование.

### 2.6 Кастомные объекты (CustomObject)

Эвристика по ВРИ:
```
для obj in options.custom_objects:
    floor = obj.floor_area_m2 or obj.plot_area_m2
    if obj.vri_code.startswith("4."):
        cost_custom += floor × 0.78      # коммерция как ВПП
    elif obj.vri_code.startswith("3."):
        cost_custom += floor × 1.40      # социальное как ДОО
    else:
        cost_custom += floor × 0.78      # по умолчанию — коммерция
```

### 2.7 Подытог зданий и сооружений (shell_total)

```
shell_total = cost_residential + cost_vpp + cost_kg + cost_sch
            + cost_parking_open + cost_parking_ml + cost_parking_ug
            + cost_social_parking + cost_sport + cost_custom_objects
```

### 2.8 Накладные (overhead)

```
networks    = shell_total × 0.10    (сети: водопровод/канализация/электро)
landscaping = shell_total × 0.05    (благоустройство)
design      = shell_total × 0.05    (проектирование)
contingency = (shell_total + networks + landscaping + design) × 0.07
              (непредвиденные)
```

**Источник:** `economy.overhead.*` в YAML.

**Суммарный мультипликатор (исправлено по аудиту #7):**
```
сети + благоустройство + проектирование = 20% от shell  → shell × 1.20
непредвиденные = 7% от (shell × 1.20)                    → 0.084 × shell
итого overhead = 0.20 + 0.084 = 0.284                    → shell × 1.284
```
То есть overhead ≈ **28.4% от shell** (не 26%, как было указано ранее).

### 2.9 Фиксированные (земля / ТУ / снос)

```
fixed = 0.0   (в v0.9.x — зарезервировано под v0.8.6 cash-flow)
```

### 2.10 Итого

```
cost.total = shell_total + networks + landscaping + design + contingency + fixed
```

---

## 3. Выручка

### 3.1 Жильё

```
revenue_residential = apartments_area × P_class
```

`P_class` из `economy.sale_prices.residential_by_class`:

| Класс | Цена/м² |
|---|---|
| economy | 1.55 |
| comfort | 1.95 |
| business | 2.80 |

**Источник:** SPEC.md (типичная маржа РФ для эконома/комфорта/бизнеса).

### 3.2 Парковки

**Аудит #4:** выручка считается только по **ожидаемо реализованным**
местам — цена × число мест × **доля реализации по классу** `sale_rate`:

```
revenue_parking_open = n_open × 3.0  × sale_rate(class)   (откр. ≈ 200-400 тыс ₽)
revenue_parking_ml   = n_ml   × 10.0 × sale_rate(class)   (МУ ≈ 0.8-1.2 млн ₽)
revenue_parking_ug   = n_ug   × 18.0 × sale_rate(class)   (подз. ≈ 1.5-2.5 млн ₽)
```

`sale_rate(class)` из `economy.sale_rates.parking_by_class`:

| Класс | Доля реализации м/м |
|---|---|
| economy | 0.20 |
| comfort | 0.45 |
| business | 0.75 |

> **Важно (асимметрия cost↔revenue):** **себестоимость** считается по
> **всем построенным** местам (§2.4), **выручка** — только по
> **реализованным**. Непроданные места уже построены и стоят денег, но
> выручки не дают. Поэтому избыточная подземка топит экономику. Это
> намеренно и соответствует рынку.

**Цены** (3.0 / 10.0 / 18.0) откалиброваны в v0.9.1 под СПб 2024;
**доли реализации** введены в v0.9.14.

### 3.3 ВПП

```
revenue_vpp = bi_area × 1.30
```
Цена коммерческой ВПП (shell+core с отделкой арендатора, м² продажи).

### 3.4 Кастомные объекты (коммерция)

Для всех custom_objects где `vri_code` НЕ начинается с `3.`:
```
revenue_custom += floor_area × 1.30
```
Социальные кастомы (3.x) → 0.

### 3.5 Компенсация ДОО/СОШ городом (v0.9.14)

```
revenue_social_compensation =
    (cost_kg_billable + cost_sch_billable) × 0.7
```
где:
- `cost_kg_billable = kg_bld × 1.40` если ДОО строит застройщик
  (не `only_demand`); иначе 0.
- Аналогично для СОШ.
- `0.7` (`economy.social_compensation.share`) — доля компенсации:
  застройщик передаёт объект городу через выкуп / КОТ / льготы.

**Аудит #6 — это допущение СЦЕНАРИЯ, а не универсальная реальность.**
Практика СПб смешанная: в 2024 из 24 построенных девелоперами школ
7 переданы безвозмездно, 15 выкуплены, 1 в аренду, по 1 нет соглашения.
Поэтому 0.7 — это сценарий «преимущественно выкуп». Разумные режимы:

| Режим | Доля | Когда |
|---|---|---|
| безвозмездно | 0.0 | объект дарится городу |
| частично | 0.5 | смешанная схема |
| **выкуп (база)** | **0.7** | преимущественно выкуп/КОТ |
| полный выкуп | 0.9 | город выкупает почти полностью |

**Без компенсации (0.0) почти любой квартал с соцобъектами убыточен** —
соцнагрузка не окупается продажей квартир. Поэтому в экономике есть
отдельная метрика `net_social_burden` (gross cost − compensation): она
показывает, выгоден ли проект **сам по себе** или только при выкупе.

> Сейчас `share` фиксирован в YAML (0.7). Выбор режима в UI —
> в планах (см. дорожную карту).

### 3.6 Итого

```
revenue.total = revenue_residential
              + revenue_parking_open + revenue_parking_ml + revenue_parking_ug
              + revenue_vpp + revenue_custom_commercial
              + revenue_social_compensation
```

---

## 4. Метрики

```
profit = revenue.total - cost.total
margin = profit / revenue.total   (если revenue > 0)
ROI    = profit / cost.total      (если cost > 0)
profit_per_site_m2 = profit / site.area_m2
```

---

## 5. Пример полного расчёта

Один физический вариант, посчитанный для **трёх классов жилья** (с учётом
обоих фиксов аудита: вычет здания встроенного ДОО + класс-фактор
себестоимости). Себестоимость теперь **слабо зависит от класса**
(через `class_factor` на жилую часть), а основная разница — в цене
продажи и доле реализации м/м.

### 5.1 Конфигурация варианта

| Параметр | Значение |
|---|---|
| Площадь квартала | **50 000 м² (5 га)** |
| Этажность | 9 эт. (референс, ставка себестоимости = 1.00) |
| ДПТ | да (потолок КИТ ≤ 2.5) |
| ДОО | **встроенно-пристроенный** (строит застройщик) |
| СОШ | **не учитывается** (`include_school=False`) |
| Парковки | **только наземные открытые** (`all_open`) |
| Спорт-сооружения | да (по умолчанию) |

Команда воспроизведения:
```python
from urban_model.models import Site, CalculationOptions, KindergartenSpec, ParkingConfig
from urban_model.normatives import load_normatives
from urban_model import solve_max_kit
norms = load_normatives("spb")
opts = CalculationOptions(
    floors=9, planning_doc=True, residential_class="comfort",  # ← меняйте класс
    include_school=False,
    kindergarten=KindergartenSpec(building_type="built_in"),
    parking=ParkingConfig(mode="all_open"),
)
r = solve_max_kit(Site(area_m2=50_000), opts, norms)
print(r.economy.profit, r.economy.margin, r.economy.roi)
```

### 5.2 ТЭП-результат (одинаков для всех классов)

| Параметр | Значение |
|---|---|
| КИТ ПЗЗ | 1.201 |
| GFA | 63 140 м² |
| Площадь квартир | 44 992 м² |
| Население | 1 606 чел |
| ДОО (встроенный) | здание ≈ 3 150 м² |
| Парковки | 563 м/м, **все открытые** (0 МУ, 0 подз.) |
| Спорт-площадки | ≈ 1 606 м² |
| Парковки соцобъектов | ≈ 208 м² |

> Достигнутая плотность ≈ **321 чел/га** (ниже норматива 450) — то есть
> площадь квартир ограничена не плотностью и не этажностью, а **балансом
> территории** при данном составе объектов. См. оговорку в §6.3.

### 5.3 Себестоимость (Cost)

Жилая часть умножается на `class_factor` (0.88 / 1.00 / 1.15), поэтому
жильё и зависящие от него накладные различаются по классу. Здание
встроенного ДОО (3 150 м²) **вычтено** из жилой GFA (аудит #1):
жилая база = 63 140 − 3 150 = **59 990 м²**.

| Статья | Формула | economy | comfort | business |
|---|---|---|---|---|
| Жильё | 59 990 × 1.00 × factor | 52 791 | 59 990 | 68 988 |
| ДОО (здание) | 3 150 × 1.40 | 4 410 | 4 410 | 4 410 |
| Парковки откр. | 563 × 20.75 × 0.05 | 584 | 584 | 584 |
| Парковки соцобъектов | 208 × 0.05 | 10 | 10 | 10 |
| Спорт-сооружения | 1 606 × 0.05 | 80 | 80 | 80 |
| **Σ shell** | | **57 876** | **65 074** | **74 073** |
| Overhead (×0.284) | shell × 0.284 | 16 437 | 18 481 | 21 037 |
| **TOTAL COST** | | **74 312** | **83 556** | **95 110** |

(ВПП, СОШ, МУ/подземные парковки = 0 в этом варианте.)

### 5.4 Выручка и метрики

Цена квартир `P_class` и доля реализации м/м `sale_rate` различаются.
Соц-компенсация = 4 410 × 0.7 = **3 087** у.е. во всех классах.

| Источник | economy | comfort | business |
|---|---|---|---|
| Жильё (44 992 × P) | 76 487 `(×1.70)` | 87 735 `(×1.95)` | 125 978 `(×2.80)` |
| Парковки откр. (563 × 3.0 × rate) | 338 `(×0.20)` | 760 `(×0.45)` | 1 267 `(×0.75)` |
| Компенсация ДОО | 3 087 | 3 087 | 3 087 |
| **TOTAL REVENUE** | **79 912** | **91 582** | **130 332** |
| TOTAL COST (см. §5.3) | 74 312 | 83 556 | 95 110 |
| **PROFIT** | **+5 599** | **+8 026** | **+35 222** |
| Margin (profit/revenue) | +7.0 % | +8.8 % | +27.0 % |
| ROI (profit/cost) | +7.5 % | +9.6 % | +37.0 % |

**Итог:** после калибровки (economy cost-factor 0.88, цена 1.70) все три
класса прибыльны: эконом слабо (+5 599, маржа 7%), комфорт уверенно
(+8 026), бизнес — сильно (+35 222). Бизнес-ROI **37%** — реалистично для
оценки «до стоимости земли». Запрошенный
«точно прибыльный» вариант — **бизнес-класс** (после калибровки прибыльны
все три класса, эконом — слабо).

---

## 6. Граница рентабельности

### 6.1 Главный рычаг — класс жилья

Класс влияет и на выручку (цена + доля реализации м/м), и теперь на
себестоимость (`class_factor`). Чистый эффект всё равно сильно
положительный к более высокому классу. Точка безубыточности по цене
квартир для комфорта (cost.total = 83 556):

```
P_break-even ≈ (cost.total − прочая_выручка) / apt
            = (83 556 − 3 087 − 760) / 44 992 ≈ 1.77 у.е./м²
```
Комфорт (1.95) уверенно выше порога. Эконом после калибровки (цена 1.70,
cost-factor 0.88, cost.total 74 312, порог ≈ 1.58 у.е./м²) — тоже выше
своего порога, поэтому слабо прибылен (+5 599).

### 6.2 Влияние этажности (тот же вариант, comfort)

Себестоимость м² жилья растёт с этажностью, **выручка — нет** (площадь
квартир упёрта в баланс/плотность, не в этажи):

| Этажи | Ставка жилья | Profit (comfort) |
|---|---|---|
| 9 | 1.00 | **+8 026** |
| 12 | 1.10 | +324 |
| 16 | 1.20 | −7 379 |
| 20 | 1.35 | −18 933 |

Вывод: для квартала, где площадь квартир ограничена плотностью, **рост
этажности только увеличивает себестоимость** → ниже этажность выгоднее.
Это контринтуитивно, но прямое следствие норматива плотности (см. §6.3).

### 6.3 Оговорка про «потолок» площади квартир

В этом варианте площадь квартир (44 992 м²) одинакова для всех этажей —
её ограничивает **баланс территории + норматив плотности**, а не
этажность. Это поведение ТЭП-слоя (вне охвата аудита экономики), но оно
напрямую влияет на экономический вывод «ниже этажи — выгоднее». Если
аудитор проверяет только экономику — достаточно принять apt как вход.

### 6.4 Прочие рычаги

| Рычаг | Эффект на profit |
|---|---|
| Класс эконом→комфорт→бизнес | главный: +5 599 → +8 026 → +35 222 |
| Соц-компенсация 0.7→0.9 | +0.2 × cost ДОО (4 410) ≈ +880 у.е. |
| ДОО `only_demand=True` | убирает cost ДОО и его overhead, теряет компенсацию 3 087; **нетто profit РАСТЁТ на ≈ +2 590 у.е.** (исправлено по аудиту #5 — раньше ошибочно стоял минус) |
| Подземные парковки вместо открытых | резко дороже (cost ≫/место), при низкой доле реализации топит экономику |
| fixed (земля) | сейчас 0; реальная земля 5–15% затрат уменьшит profit |

> **Аудит #5 (важно):** убрать ДОО из стройки (`only_demand`) **повышает**
> прибыль, а не снижает — у ДОО чистая нагрузка положительна
> (cost > компенсация). Чистая нагрузка ДОО ≈ 4 410 × 1.284 − 3 087 ≈
> +2 574 у.е., и расчёт это подтверждает (+2 590).

---

## Источники в коде

Главный модуль:
- `src/urban_model/economy/cost.py` — формулы себестоимости
- `src/urban_model/economy/revenue.py` — формулы выручки
- `src/urban_model/economy/metrics.py` — profit/margin/ROI

Коэффициенты:
- `configs/spb.yaml` секция `economy:` — все ставки и коэффициенты

Тесты:
- `tests/test_economy.py` + `tests/test_custom_economy.py` (25 тестов).
- Весь набор после фиксов аудита: **375 passed**.

---

## Что НЕ моделируется (намеренно)

- **Cash flow / NPV / IRR / Payback** — отложено на v0.9.16 (требует
  timeline_months, discount_rate, sales_curve).
- **Земля / ТУ / снос** — поле `fixed = 0` (зарезервировано).
- **Налоги** — модель «до налогов» (PRE-tax).
- **Финансирование** (кредиты, проценты) — НЕ учитывается.
- **Инфляция / временная стоимость денег** — единовременный расчёт.

---

**Версия документа:** v0.10.20 (2026-05-31).
**Контактный модуль:** `src/urban_model/economy/`.
**Пример пересчитан:** 5 га · 9 эт. · встроенный ДОО · без СОШ ·
парковки `all_open` · классы эконом/комфорт/бизнес.
**Калибровка v0.10.20:** economy cost-factor 0.95→0.88, цена 1.55→1.70;
шкала интерпретации sellable_ratio в UI.
