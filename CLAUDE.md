# my_urban_model

Математическая модель застройки территории для оценки вариантов проектных решений и расчёта ТЭП. Замена связки Grasshopper + Galapagos для математической части задачи (без геометрии на старте).

## Контекст

- Автор: Михаил, фоновое образование в градостроительстве (опыт работы с GIS в СПб).
- Работает независимо, проект для собственного использования.
- Раньше использовал Grasshopper + Galapagos. Уходит от этого в сторону Python ради гибкости, версионирования через git и более мощных оптимизаторов.
- Проект живёт в `D:\Github\my_urban_model\`. Зеркало: https://github.com/misht-world/my_urban_model
- Источник истины по задаче — `info/ТЗ_обратный_расчет_ТЭП.docx`. Параметры — `info/Параметры застройки территории.docx`. ВРИ-справочник — `dict/dict_vri.yaml`.

## Цель

**Обратный расчёт ТЭП.** Пользователь задаёт площадь квартала, регион (СПб), ограничения и состав учитываемых объектов; программа подбирает максимально допустимый КИТ, при котором выполняется баланс территории и нормативные требования, и возвращает связанные ТЭП.

Прямой расчёт (`compute_tep_for_kit(kit, ...)`) — внутренняя функция; обёрнута в публичный режим `verify_kit` для проверки заданного КИТ.

## Архитектура (5 слоёв)

```
configs/        — YAML: нормативы по ВРИ-кодам. Цифры живут только тут.
src/urban_model/
  normatives/   — Pydantic-схема, loader (russia↔spb наследование), resolver.
  models/       — Site, Housing, Social, Parking, DecisionVariables, TEPField, TEPResult, Scenario.
  calculations/ — чистые функции без побочных эффектов. Ядро.
  core/         — forward (compute_tep_for_kit) и inverse (solve_max_kit, бисекция).
  modes/        — публичные точки входа: inverse, verify, compare.
  export/       — TEPResult → DataFrame / xlsx с цветовой маркировкой статусов.
```

Принцип: расчётное ядро не знает ничего про режимы работы. Один движок — все режимы.

## Стек

| Компонент | Технология |
|---|---|
| Язык | Python 3.11+ |
| Менеджер зависимостей | uv |
| Валидация | Pydantic v2 |
| Конфиги | PyYAML |
| Таблицы | Pandas |
| xlsx | openpyxl |
| DOCX-отчёт | python-docx + matplotlib (с v0.10) |
| Тесты | pytest |
| Демо | Jupyter |
| Оптимизация одно-крит. | Optuna (с v0.5) |
| Оптимизация мульти-крит. | DEAP / NSGA-II (с v0.6) |

Геометрия (Shapely/GeoPandas) и UI — за рамками v0.1–v1.0.

## Нормативы — структура

Двухуровневая система YAML: `configs/russia.yaml` (база, СП 42.13330.2016) + `configs/spb.yaml` (`parent: russia`). Регион переопределяет только то, что отличается; недостающее наследуется от базы.

**Ось организации — ВРИ-код** (Приказ N П-0412). Озеленение и парковки в ПЗЗ СПб структурированы именно так. Семантические алиасы (`kindergarten = "3.5.1"`, `housing = "2.6"`) — поверх, для удобства.

Каждый параметр имеет явный `type`:

- `scalar` — простое число (мест/1000 = 61, КИТ_max = 2.5).
- `piecewise` — кусочно-постоянная функция от аргумента (площадь участка ДОУ от вместимости; ЗНОП от КИТ).
- `conditional` — зависит от категориальной переменной (мин/макс вместимость ДОО от building_type).

Каждый параметр обязательно содержит `source` — ссылку на нормативный документ (СП, НГП, ПЗЗ, РМД, письмо). Без источника параметр не принимается — он юридически невалиден.

В коде универсальный интерфейс:

```python
norms = load_normatives(profile="spb")
plot = norms.resolve("social_objects.kindergarten.plot_area_per_place",
                     capacity=180)  # → 40 (попадает в "до .inf" ступень)
kit_max = norms.resolve("kit_limits", planning_doc="yes")  # → 2.5
```

## TEPResult — структура

Каждое поле результата — это `TEPField`, а не голое число:

```python
TEPField(
    value: float,            # принятое к расчёту значение
    normative: float | None, # нормативное (без округлений и ручных правок)
    user_value: float | None,# пользовательское (если задано вручную)
    status: Status,          # ok / warning / error / manual / no_data
    source: str | None,      # ссылка на документ
    formula: str | None,     # текст вида "население × 61 / 1000"
)
```

Это закрывает требования ТЗ: историю расчёта (§19.1), статусы показателей (§19.3), видимость ручных правок (§12), ограничивающий фактор в `solve_max_kit`.

## Парковочные сценарии

`ParkingConfig.mode` — три режима, дёргают одну и ту же функцию `compute_parking_breakdown`:

- `min_open` — открытых ровно норматив-минимум (12.5%), остальное в подземных. По умолчанию.
- `all_open` — 100% открытые. Максимальная нагрузка на поверхность квартала.
- `custom` — пользователь задаёт три доли (open + multilevel + underground = 1.0).

Многоуровневые наземные паркинги дробятся по нормативу `multilevel_capacity_max` (300 м/м для СПб). Пятно вычисляется по piecewise `multilevel_area_per_place(levels)` и **попадает в баланс квартала отдельным компонентом**. Подземные не занимают поверхностной площади.

## Публичный API

```python
from urban_model import (
    solve_max_kit,
    solve_max_kit_with_reserve,
    solve_max_kit_with_znop,
    verify_kit,
    compare_scenarios,
    run_scenarios,
)
from urban_model.models import (
    Site, CalculationOptions, Scenario, ParkingConfig, BuiltInArea,
)
from urban_model.normatives import load_normatives
from urban_model.export import to_xlsx, results_to_dataframe
from urban_model.optimize import SearchSpace, optimize_max_apartments  # v0.5
```

## Оптимизатор (v0.5)

Galapagos-аналог на Optuna. Цель — максимизация площади квартир при выполнении всех ограничений (балансовый surplus ≥ 0, плотность в норме). Декрейшн-переменные: этажность, режим парковок и доли open/multilevel/underground, этажность многоуровневых паркингов, число ДОО, число СОШ, ВПП on/off.

Каждое испытание Optuna строит `CalculationOptions` поверх базового шаблона и вызывает `solve_max_kit` — то есть оптимизатор работает **поверх обратного расчёта**, а не вместо него. Это держит ядро вычислений простым: оптимизация — отдельный слой.

```python
from urban_model.optimize import SearchSpace, optimize_max_apartments

space = SearchSpace(
    floors_range=(8, 25),
    parking_modes=["min_open", "all_open", "custom"],
    parking_open_share_range=(0.1, 0.5),
)
report = optimize_max_apartments(site, base_options, norms, space, n_trials=100)
print(report.best.apartments_area, report.best.params)
for r in report.top_n[:5]:
    print(r.rank, r.kit, r.params)
```

В UI (вкладка «🧬 Оптимизация») — форма выбора варьируемых параметров + слайдер trials + таблица топ-N + кнопки «Добавить в сравнение».

## Определение КИТ (важно!)

Текущая модель использует определение КИТ из ПЗЗ СПб:
```
КИТ = площадь квартир / площадь ЗУ жилой застройки
```
Это значение лежит в `result.kit` и сравнивается с нормативом (≤ 1.4 без ДПТ; ≤ 2.5 с ДПТ). К нему же применяется piecewise-функция ЗНОП.

Внутренняя переменная бисекции — `block_density` = `GFA / S_квартала` — **не путать** с КИТ. Доступна как `result.block_density` для аудита/отладки.

## Дорожная карта (актуализирована 2026-05-10)

| Версия | Содержание | Статус |
|---|---|---|
| **v0.1** | uv-скелет; нормативы (russia+spb) с piecewise/conditional/наследованием; `TEPField/TEPResult`; чистые расчёты ТЭП по ТЗ; `solve_max_kit` через бисекцию; ДОО + СОШ; тесты; demo-notebook. | ✅ |
| **v0.2** | `verify_kit`, `compare_scenarios`/`to_xlsx`, `solve_max_kit_with_reserve`, `solve_max_kit_with_znop`, **ВПП как сущность** (`BuiltInArea`). | ✅ |
| **v0.3** | Парковочные сценарии (open/multilevel/underground); WARNING при СОШ < нормативного минимума; **Streamlit-UI** (sidebar + KPI + expanders + сравнение + xlsx). | ✅ |
| **v0.5.0** | **Optuna-оптимизатор** (`src/urban_model/optimize/`): `SearchSpace` + `optimize_max_apartments`. Вкладка «🧬 Оптимизация» в UI. Override ДОО/СОШ в UI. | ✅ |
| **v0.5.1** | **Контроль норматива 25% озеленения квартала** в `balance.is_feasible` (раньше был справочной величиной). Хотфикс CLI `run.py`. SchoolSpec defaults False/False. | ✅ |
| **v0.5.2** | `Scenario.mode` расширена до 4 режимов (with_reserve, with_znop). Optuna exception reporting + ВПП-trap warning. | ✅ |
| **v0.5.3** | UI текст: ДПТ, ЗНОП, ДОО, парковки, СОШ message — нормальные русские лейблы. | ✅ |
| **v0.5.4** | **Переопределение КИТ** на ПЗЗ-формулу `apartments_area / housing_lot`. ZNOP теперь по правильному КИТ. Внутренний `block_density` (GFA/site) — отдельное поле в TEPResult. | ✅ |
| **v0.5.5** | `CustomObject` — список произвольных объектов с ВРИ-кодом (офис/ФОК/поликлиника). Каждый занимает свой ЗУ, требует парковок и даёт озеленение по ВРИ. | ✅ |
| **v0.5.6** | Равномерное распределение мест в ДОО/СОШ (`distribute_places_evenly`). Фикс ParkingConfig validation в Optuna. Оверрайд долей проездов в UI. Отдельная вкладка «📦 Объекты» с табличным редактором. Улучшена ясность UI оптимизатора парковок. | ✅ |
| **v0.5.7** | Аудит-фиксы: `__init__` версия и `__all__`, валидация capacity_min/max, magic numbers убраны из formula-строк, дорожная карта актуализирована, документировано ограничение CustomObject parking. | ✅ |
| **v0.6.4** | UX-батч (9 пунктов): lock-icon, ДПТ выше этажности, крупные вкладки, KPI-ряд2 (ДОО/СОШ/паркинг/ЗНОП), CSS-уплотнение, ЗНОП-лейблы, ВПП в блок жилья, пресет 50/50, превью топ-N оптимизации. | ✅ |
| **v0.6.5** | **Встроенно-пристроенный ДОО**: ЗУ = 24 м²/место (ПЗЗ СПб); здание ДОО вычитается из жилой GFA. Фикс счётчика вкладки «Сравнение». «N объектов по M мест» в KPI. CSS-вкладки — крупнее и выразительнее. capacity_min/max для built_in ДОО: 120/350. **СОШ-нормативы по данным КС** (параллели II–IX): rounding=25 (СП Градостроительство), capacity_max=2475, новый список allowed_capacities, обновлены plot_area/building_area piecewise. WARNING если вместимость СОШ не из списка [550/825/1100/1375/1650/1925/2200/2475] — с указанием ближайших. Аудит-фиксы (17 пунктов): magic numbers убраны в YAML, `type: list` в схеме нормативов, общий хелпер `allowed_capacities.build_warnings`, источники через `norms.source_of()`. | ✅ |
| **v0.6.6** | **«Только потребность»** для ДОО и СОШ (`KindergartenSpec.only_demand` / `SchoolSpec.only_demand`): объект учитывается для подсчёта мест, но ЗУ/здание не входят в баланс квартала и не вычитают жилую GFA. **Piecewise `driveways.housing_lot_share` по этажности**: ≤4 эт. → 100% от застройки, >4 → 120%. | ✅ |
| **v0.6.7** | **Редизайн вкладки «Параметры»**: двухколоночная компоновка. Слева — «Общие сведения о территории» (площадь, ДПТ, этажность, expander «Проезды на ЗУ») и список чекбоксов «Учитывать в расчёте». Справа — плитки настроек для каждого включённого компонента. Внутренние чекбоксы «Учитывать ДОО/СОШ/ЗНОП/ВПП» убраны из плиток (управляются снаружи). **Новые флаги** `include_parking`, `include_znop`, `include_intra_driveways` в `CalculationOptions`: при `False` компонент = 0 в балансе/ЗУ/озеленении. | ✅ |
| **v0.6.8** | **Плоскостные спортивные сооружения (ВРИ 5.1.3)**: 1000 м²/1000 чел + 40% озеленения (ПЗЗ); до 49% озеленения замещается самой спортплощадкой (п. 1.9.4 ПЗЗ). Новый модуль `calculations/sport.py`, секция `sport_facilities` в `spb.yaml`, `SportFacilitiesSpec` + `include_sport_facilities` (по умолчанию True). Компонент в balance.components, плитка в UI с чекбоксом «только потребность». Пример: 1000 чел → ЗУ = 1204 м². | ✅ |
| **v0.6.9** | **Override площади спортсооружений** (`SportFacilitiesSpec.area_override_m2`): задать вручную в м² или га; озеленение и ЗУ пересчитываются от заданной площади. **UI-правки**: СОШ-чекбоксы (бассейн/спорт-ядро) вертикально, ВПП-radio без «(рассчитать)». | ✅ |
| **v0.7.0** | **Парковки ДОО и СОШ** по формуле ПЗЗ СПб: `max(2, ceil(workers/5) + ceil(students/100))` на каждый объект. Работники по вместимости — piecewise из данных КС (`workers_per_capacity` в `spb.yaml`). Норматив парковки — `parking.social_objects`. **Архитектурно**: парковки соцобъектов — отдельные открытые на собственном ЗУ соцобъекта; в общий пул жилищных НЕ вливаются. Площадь = `places × 20.75 м²` входит в баланс как компонент `social_parking_plot` и вычитается из знаменателя 25%-озеленения. При `only_demand` / `include_*=False` — не учитываются. | ✅ |
| **v0.7.1** | **Список ВПП** с 5 вариантами размещения: full_floor / half_floor / min_only / min_plus / custom_only. Обязательные ВПП по НГП СПб: 4.4 торговля (460.10 м²/1000), 4.6 общепит (105.20 мест × 6 м²), 3.3 быт.обсл. (19 раб.мест × 20 м²), 3.4.1 поликлиника (26.33 посещ × 8 м²), 3.5.1 школа искусств (7.20 мест × 15 м²). Новый модуль `calculations/vpp.py` с `compute_mandatory_areas` + `build_built_ins`. `built_in_list: list[BuiltInArea]` в `CalculationOptions`. UI: радио на 5 режимов с условными полями custom 4.4/4.6 + превью обязательных площадей. Двухпроходный расчёт в `state.run_calculation` (footprint → построение списка → расчёт). Парковки ВПП — **единый средний коэф** 1.56 м/м на 100 м² = 64 м²/м.м (вместо детальной формулы по каждому ВРИ; упрощение по запросу). | ✅ |
| **v0.7.2** | **UX-батч** по результатам тестов: цветовая дифференциация левой/правой колонок «Параметры» (синий/зелёный фон), убраны конкретные цифры из подсказок проездов, убрана длинная caption спортсооружений. **Основные показатели** на вкладке «Расчёт» — в едином блоке с кнопкой копирования сводки в текст. **Плотность** теперь по формуле СП 42.13330 (S_квартир/20 м²/чел / S_квартала). Статусы TEPField на русском (норм./внимание/ошибка/вручную/нет данных). Формулы основных полей переписаны на читаемый русский («Общая площадь зданий (GFA)»). «Добавить в сравнение» переставлено под KPI. Снят отдельный пункт «сверка parking.vpp» — заменён единым средним коэф в v0.7.1.1. | ✅ |
| **v0.7.3** | **UX-батч-2**: усилена цветовая дифференциация колонок «Параметры» (border-left 4px). «Добавить в сравнение» теперь ВНУТРИ блока «Основные показатели». Кнопка копирования сводки — JS-clipboard в буфер ОС одной кнопкой + inline-сообщение «✓ Скопировано». Статус «норм.» → «ok». **Редизайн вкладки «Оптимизация»**: 2-колоночная компоновка (галочки слева, плитки настроек справа), убран expander «Что варьировать», убрана отсылка к Galapagos, парковки/ВПП — галочки вместо selectbox, **добавлен ЗНОП как варьируемый параметр** (`SearchSpace.znop_per_person_choices`), trials/seed скрыты (фиксировано 2000 trials ≈ 20с). | ✅ |
| **v0.7.4** | **Фикс ЗНОП-КИТ связи**: при `znop_per_person_override` теперь применяется обратная piecewise по ПЗЗ — `effective_kit_max = min(kit_limits, znop.kit_cap_for_znop(z_pp))`. ЗНОП=0 → КИТ ≤ 1.59; ЗНОП=3 → ≤ 1.79; ЗНОП=4 → ≤ 1.99; ЗНОП=6 → ≤ 2.50. Раньше override обходил норматив, и оптимизатор находил КИТ=2.5 даже при ЗНОП=3, что противоречит ПЗЗ. Новая функция `znop.kit_cap_for_znop()`. | ✅ |
| **v0.8.0** | **Экономика в условных единицах** (MVP): новый модуль `src/urban_model/economy/` с `calc_cost`/`calc_revenue`/`calc_metrics`. Базовая единица: 1.0 у.е. = м² жилья 9 эт. монолит standard. Себестоимость по типам зданий (residential piecewise по этажности, ВПП, ДОО, СОШ, парковки с прогрессией подземных уровней) + overhead (сети, ландшафт, проект, непредвиденные). Выручка по классам жилья (economy/comfort/business 1.55/1.95/2.80) и типам м/м. Метрики: profit / margin / ROI / profit_per_site_m2. Поле `TEPResult.economy: EconomicMetrics`. UI: KPI-блок «💰 Экономика» в основных показателях + expander «Себестоимость / Выручка / Метрики». Optuna: новый параметр `SearchSpace.objective` ("apartments_area" \| "profit"), UI radio «Цель». Источник коэффициентов — `my_development_calc/SPEC.md`. | ✅ |
| **v0.8.1–v0.8.4** | UX-фиксы оптимизатора: цветовая дифференциация колонок, парковочные слайдеры, ВПП-режимы галочками, ЗНОП ступенями, фикс счёта ДОО/СОШ в таблице топ-N (`_count_buckets` парсит formula), `strict_social_validation` для отсева невалидных вместимостей. | ✅ |
| **v0.8.5** | **Аудит P0-фиксы** (`AUDIT.md`): (1) дубль площади открытого м/м в YAML — economy теперь читает `parking.open_space_per_place`; (2) удалён зомби-код `fixed = land_cost + …` (поля не существовали); (3) `EconomicMetrics` через `TYPE_CHECKING` + `model_rebuild()` — снижена связность ядра; (4) новый `calculations/warning_codes.py` с `WC`-enum и тегами `[CODE] …`; Optuna фильтрует по кодам, не по русским подстрокам; (5) WARNING по плотности подземки (>400 м/м/уровень → `PARKING_UG_OVERPACKED`); (6) экономика учитывает `social_parking`, `sport`, `custom_objects` в `CostBreakdown`, кастомные коммерческие — в `RevenueBreakdown`; (7) кэш preview-solve для ВПП-режимов в Optuna (раньше 2× `solve_max_kit` на trial → 1×). Тесты: 284 → 290. | ✅ |
| **v0.8.5–v0.8.9** | Аудит P0-фиксов + мягкие нормативы озеленения/плотности + UI-батч (свёртываемые блоки, 2-колонка тайлов, крестик закрытия, toggle для «только потребность»). | ✅ |
| **v0.9.0** | **Редизайн вкладки «Оптимизация»**: вместо 20 ручек + 2000 trials Optuna — 3 секции: (1) snapshot базы с вкладки Расчёт; (2) 3 рекомендации Optuna по 400 trials (Max площади / Max прибыли / Сбалансированный) с дельтами от базы; (3) пофакторный анализ — 3 детерминированных скана (Парковки 11 точек / ЗНОП 4 ступени / Этажность 21 точка) с altair-графиками. Старая форма с 20 ручками сохранена в свёрнутом expander «⚙ Продвинутый режим». Новые модули `optimize/scans.py` (`scan_parking_underground_share`, `scan_znop_steps`, `scan_floors`) и `optimize/pareto.py` (`generate_pareto_recommendations`, `DeltaSummary`, `Recommendation`, `ParetoBundle`). `ui/state.py` сохраняет `last_calc_result` в session_state для синхронизации между вкладками. Тесты: 290 → 306 (+test_scans.py, +test_pareto.py). | ✅ |
| **v0.9.1** | Калибровка цен парковок (3.0/10.0/18.0), дедуп Парето-рекомендаций по архетипу парковки, разнообразие через RandomSampler. | ✅ |
| **v0.9.2** | Фикс синхронизации Расчёт↔Оптимизация (last_calc_options сохраняется ОРИГИНАЛЬНЫМ до VPP-постобработки); зелёный ромб рекомендации (int 0/1 для altair-фильтра). | ✅ |
| **v0.9.3** | Расширенный snapshot базы; `ParetoConstraints` (диапазон этажности + разрешения типов парковок). | ✅ |
| **v0.9.4** | Убран ЗНОП из вариаций Парето — норматив piecewise(КИТ) даёт корректные ступени. | ✅ |
| **v0.9.5** | Footer «Сценарий» в карточках; scatter «apt×profit» (потом удалён в v0.9.6). | ✅ |
| **v0.9.6** | Единый формат KPI везде; сравнительная таблица база↔рекомендации с подсветкой лучшего. | ✅ |
| **v0.9.7** | «Локальный анализ» в сканах; ДВА оптимума («по площади» и «по прибыли») в резюме. | ✅ |
| **v0.9.8** | Аудит-фиксы P0/P1: калибровка ceбестоимости парковок (0.05/0.30/0.40), симметрия cost↔revenue для custom-объектов, VPP-кэш учитывает мягкие нормативы, восстановление парковок в `_rec_options_from_params`. | ✅ |
| **v0.9.9** | Фильтр типологически редких гибридов МУ+подземные (>10% каждого) в Парето. | ✅ |
| **v0.9.10** | Фикс ValidationError ParkingConfig (нормализация долей); минимальные пороги мест МУ/подземка (50/30). | ✅ |
| **v0.9.11** | Полный аудит логики: `no_feasible_reason` при 0 рекомендаций, подсказка про `only_demand` при малом населении, логирование скан-исключений, фикс `_select_three` fallback, `_is_hybrid_parking` граница `>=`, multilevel_levels сохраняются всегда. Тесты 306 → 316. | ✅ |
| **v0.9.12** | clamp ZNOP>6, расширение подземной прогрессии до 5 уровней, расширение бисекции (×64), адаптивные пороги токенов (max(абс, 5% от total)), динамические ЗНОП-ступени из YAML, caption «КИТ упёрт в потолок», маржа/ROI в KPI-таблице, тесты экономики custom_objects. Тесты 316 → 322. | ✅ |
| **v0.9.13** | UI-улучшения: snapshot базы — 3 ряда `st.metric` (вместо markdown-таблицы), TL;DR-строка над карточками Парето, bold ключевых строк в comparison-table. | ✅ |
| **v0.9.27** | **Экономика P1 (по внешнему аудиту):** (1) `sale_rate` парковок по классу жилья (`economy.sale_rates.parking_by_class`: эконом 0.20 / комфорт 0.45 / бизнес 0.75) — выручка м/м = места × цена × доля реализации (раньше считалось 100% продаж, завышая выручку подземки); (2) блок «Социальная нагрузка» в Экономике (`net_social_burden`, `profit_before_social`); (3) `profit_per_site_m2` показан как «Запас / м²» — основная метрика сравнения территорий; (4) `sellable_ratio` (выход жилья) в KPI; (5) `profit_before_land` (резерв под стоимость земли). Тесты 322 → 328. | ✅ |
| **v0.9.28 (ядро, Фаза A)** | **Кластеры этажности — ядро**: модель `FloorCluster(area_m2, floors, floors_min/max)`, поле `CalculationOptions.floor_clusters` (пусто → старое поведение). `floors_eff = Σ(A·F)/ΣA` (`calculations/clusters.py`) для пятна застройки/баланса/озеленения; проезды — средневзвешенная piecewise по площади; экономика жилья — покластерно по ставкам этажности; покластерный КИТ_i = КИТ_global × F_i/floors_eff; потолок КИТ ×`floors_eff/max(F_i)`. Поля `TEPResult.effective_floors` + `floor_clusters_detail`. WC.CLUSTER_AREA_MISMATCH. **UI (Фаза B):** редактор зон в «Параметры» (expander «🏗 Разная этажность по зонам», `st.data_editor` до 3 зон, метрики Σ-площадь/средневзвеш.-этажность + warning при расхождении); блок «🏗 Кластеры этажности» в результатах (покластерный КИТ, площадь квартир, пятно); кластеры в имени сценария. **Фикс v0.9.28.1:** убрано поджатие потолка КИТ под самый высокий кластер (линейная формула КИТ_i ∝ F_i неверна — КИТ задаётся средневзвешенной этажностью, озеленение/парковки ∝ площади квартир насыщают его) — давало ложный «огромный резерв» при разноэтажных зонах. Теперь КИТ **общий** (проверяется против норматива), покластерный КИТ — справочная честная декомпозиция (Σ площадей квартир сходится). Тесты 328 → 342 (+test_clusters.py). | ✅ |
| **v0.9.29 (Фаза C)** | **Optuna варьирует этажность зон**: `SearchSpace.vary_cluster_floors` — при заданных кластерах подбор перебирает этажность каждой зоны в её `[floors_min, floors_max]` (площади фиксированы); глобальный `floors_range` игнорируется. `_vpp_preview_key` учитывает этажность зон (нет коллизий кэша). Парето авто-включает режим при наличии кластеров у базы; `key_changes` показывает «Этажность зон: 9,12 → 21,21»; fingerprint дедупа учитывает зоны. `scan_floors` при кластерах сканирует единую этажность (база = средневзвеш.). **UI-батч v0.9.29:** соцобъекты вынесены отдельно в «Оценке выгодности» (Без соц. / Соц. нагрузка); per-зонная разбивка территорий в таблице баланса и сводке; имя сценария с зонами + классом жилья; убрана ссылка на ECONOMY_AUDIT.md. **UI-батч-2:** КИТ по зонам — чип под метрикой (как ДОО/СОШ); колонки «Этаж. мин/макс» в редакторе зон (диапазон перебора по зонам); этажность в snapshot/карточках/сравнительной таблице читается из TEP (cluster-aware `_floors_label`); при включённых зонах поля «Площадь»/«Этажность» сверху блокируются, площадь = Σ зон; `CalculationOptions.include_economy` — при False экономика не считается и скрыта на всех вкладках. Тесты 342 → 347. | ✅ |
| **v0.9.30** | **Карточка «ДОО/СОШ»** в пофакторном анализе (`scan_kindergarten_objects` 1..5 / `scan_school_objects` 1..3): площадь квартир от числа объектов НЕ зависит (суммарный ЗУ ≈ const×мест), поэтому карточка — **таблица реализуемости** (вместимость/объект, ЗУ, валидно ли по нормативу мин/макс), а не график; рекомендуется минимум объектов с допустимой вместимостью. **«Применить к Расчёту»**: кнопка на карточках Парето кладёт `applied_options` в session_state → вкладка «Расчёт» считает по этому сценарию (override, переносит этажность/зоны/парковки целиком) с баннером и кнопкой «↩ Вернуть форму». Надёжнее патча виджетов (custom-парковки через count×capacity не патчатся). Тесты 347 → 350. | ✅ |
| **v0.9.31 (аудит)** | **P1:** (1) `split_into_objects` (ДОО+СОШ) теперь честно учитывает `num_objects` без явной `capacity_per_object` — раньше игнорировался → скан числа ДОО/СОШ и Optuna-размерности `kg/school_num_objects` были no-op; (2) редактор зон расширяет диапазон подбора под принятую этажность зоны (`floors_min≤floors≤floors_max`). **P2:** `sellable_ratio` = квартиры / **общая** GFA (раньше gfa−ВПП ≈ const); комментарий о намеренном дубле `effective_floors`. Тесты 350 → 353. | ✅ |
| **v0.9.15** | **Sensitivity analysis (tornado)**: `optimize/sensitivity.py::compute_sensitivity` поверх сканов — размах площади квартир при изменении одного фактора (этажность/подземн./многоуровн./ЗНОП) от единой базы; ранжировано по влиянию. UI-секция «📊 Чувствительность» в Оптимизации (altair-tornado + «сильнее всего влияет»). Тесты +5. | ✅ |
| v0.9.16 | Cash flow / NPV / IRR / Payback по timeline_months + discount_rate + sales_curve. | ⏳ |
| **v0.10.0** | **DOCX-отчёт**: `export/docx_report.py::build_report(scenarios, path)` — Word-документ (таблица ключевых ТЭП + matplotlib-диаграмма площадь/выгодность + рекомендация лучшего по выгодности среди feasible). Кнопка «📄 Скачать отчёт (DOCX)» во вкладке «Сравнение». Новые зависимости: `python-docx`, `matplotlib`. Тесты +4. | ✅ |
| **v0.10.1** | Полировка: DOCX-отчёт генерируется ЛЕНИВО (по кнопке «Сформировать», не на каждый ре-рендер — matplotlib/docx больше не тормозят вкладку «Сравнение»); тест воспроизводимости «Применить к Расчёту» (`_rec_options_from_params` + solve == карточка). Тесты 362 → 363. | ✅ |
| **v0.10.4** | Подбор этажности зон — **диапазон по КАЖДОЙ зоне отдельно** (`ParetoConstraints.cluster_floors_ranges: tuple[(lo,hi),…]`). Optuna перебирает высотность каждой зоны в своём окне. Тесты 367 → 368. | ✅ |
| **v0.10.5** | UX: диапазон этажности зон в «Настройки подбора» — **range-слайдеры на зону** (по одному, с подписью «Зона N (текущая K эт.)») вместо таблицы — единообразно с единой этажностью. | ✅ |
| **v0.11.0** | **Альбом-презентация (PPTX), Фаза 1** (по `ALBUM_PLAN.md` / `info/ТЗ_отчёты.docx`): пакет `export/album/` — `build_variant_album(name, tep, options, path)`, 12 слайдов 16:9 в стиле «Спецификация» (графит+амбер+белый): титул → резюме (KPI+вердикт) → исходные условия → главные ТЭП → баланс (stacked-bar) → жильё → соц → парковки (donut) → озеленение/спорт/проезды → экономика (waterfall) → риски → что выбрать. Модули: `theme` (токены/хелперы), `charts` (matplotlib в палитре), `narrative` (авто-выводы §10), `risks` (движок рисков с порогами низкий/средний/высокий), `slides`, `builder` (try/except на слайд — §12). Кнопка «Сформировать альбом» (PPTX) вместо «Отчёт (DOCX)» на «Расчёте»; **DOCX-кнопки убраны** и на «Сравнении» (там пока XLSX, сравнительный альбом — фаза 2). Палитра §5.2 ТЗ (тёмно-синий) отменена. PDF не делаем. Тесты +7 (`test_album`), всего 390. | ✅ |
| v0.10.19–20 | UX-батч (Material Symbols Sharp вместо эмодзи, кастомные алерты с иконками, единый padding/заголовки, дефолты парковок 50/50, ползунки подбора от Базы) + калибровка эконом-класса по внешнему аудиту (cost-factor 0.88, цена 1.70, шкала sellable_ratio). | ✅ |
| **v0.10.18** | **Редизайн UI — стиль «Минимал · Спецификация»** (по `DESIGN_PLAN.md`, утверждён заказчиком): белый фон, секции-карточки без синего акцента сверху (тонкая рамка), заголовки секций — мелкий UPPERCASE с жирной графитовой чертой снизу; KPI (`st.metric`) — тонкие крупные моноширинные цифры, между колонками вертикальные волосяные линии (CSS `:has`); кнопки прямоугольные с чёрным контуром (hover — заливка графитом); активная вкладка — амбер-подчёркивание без синей заливки. Баланс территории (treemap) и логика расчётов не тронуты. Лендинг переведён в тот же стиль ранее. | ✅ |
| **v0.10.17** | **Фиксы по внешнему аудиту экономики** (`ECONOMY_AUDIT.md`): (#1, баг) устранён двойной учёт встроенного ДОО — `cost.py` вычитает площадь здания built-in ДОО из жилой GFA (раньше оболочка облагалась и как жильё ×C_base, и как ДОО ×1.40); (#2) введён `economy.construction.residential_class_factor` (economy 0.95 / comfort 1.00 / business 1.15) — бизнес перестал выглядеть чрезмерно прибыльным (ROI ~49%→37%); (#4/#5/#7) док-фиксы: формула парковок `× sale_rate`, верный знак эффекта `only_demand` (profit РАСТЁТ), overhead ≈28.4% (не 26%); (#6) соц-компенсация 0.7 документирована как сценарий выкупа. Тесты 375 passed. | ✅ |
| **v0.10.16** | Таблицы KPI-карточек рекомендаций → `st.dataframe(use_container_width)` (на всю ширину карточки); bump CSS-маркера для инвалидации кэша стилей. | ✅ |
| **v0.10.14–15** | Единообразие кнопок (равная ширина в ряду + спейсер, рамки download-кнопок), плоские маркеры сканов, синий акцент сверху карточек, теги-pills лендинга (не выглядят кнопками). | ✅ |
| **v0.10.13** | **Лендинг** `лендинг.html` + `docs/index.html` (GitHub Pages, синий+амбер, hero, карточки, шаги, скриншоты, аккордеон-описания; CTA «Открыть программу» → Streamlit Cloud). **`share.bat`** — публичная ссылка через cloudflared. Хостинг: GitHub Pages (лендинг) + Streamlit Community Cloud (программа), автодеплой по push. Фирменный стиль: амбер-акцент под заголовком и на активной вкладке. | ✅ |
| **v0.10.12** | PPTX-доработка: бейдж раздела и номер в шапке, амбер-акцент под шапкой, KPI-карточки с верхней цветной полосой, колонтитул с вариантом на каждом слайде. | ✅ |
| **v0.10.11 (фикс)** | Устранено **устаревшее предупреждение о плотности** при встроенном ДОО: warning о плотности добавлялся ДО вычитания здания встроенного ДОО из жилой GFA (показывал, напр., 474), хотя итоговая плотность уже ≤ норматива (449.9). Теперь предупреждение формируется по ИТОГОВОМУ значению; статус плотности после корректировки учитывает `enforce_density_norm`. +1 тест. | ✅ |
| **v0.10.10 (UX+PPTX v2)** | (#1) КИТ по зонам — caption прямо под метрикой «КИТ (ПЗЗ)»; (#3) кнопки выгрузки на «Расчёте» вплотную (use_container_width в узких колонках, без разрывов); (#2) **PPTX переработан**: без теней, 6 слайдов (Титул → Сводка со всеми ключевыми данными → Баланс с цветной диаграммой без наложения текста → Жильё/соц таблица → Парковки+кластеры таблицы → Экономика последней с простой диаграммой себестоимость→выручка→прибыль). Диаграммы баланса/экономики переделаны (цветные столбцы, читаемые подписи). | ✅ |
| **v0.10.9 (UX+PPTX)** | (#2) убрана подсказка про ДОО/СОШ-минимум; (#1) добавлено пояснение-инфо «Резерв обусловлен нормативом плотности 450 чел/га» с подсказкой про нежилые объекты; (#3) «СОШ: число корпусов» → «число объектов»; (#5) кнопки на «Расчёте» поджаты влево (имя+Добавить и xls+отчёт кластеризованы слева); (#4) **PPTX-презентация** `build_variant_pptx(name, tep, path)` — слайды 16:9 (титул + ТЭП + баланс + экономика), образец `пример_отчёта.pptx`. Новая зависимость `python-pptx`. Тесты +1. | ✅ |
| **v0.10.8 (UX)** | Батч по замечаниям: (#1) карточка ДОО/СОШ — пояснение под таблицей как вывод, убран warning «ни одно число…»; (#2) маркеры сканов — синий=база/красный=площадь/жёлтый-крупный=прибыль (плоские, без теней), плоская HTML-легенда; (#3) единая этажность в подборе с 3 эт. (как зоны); (#4) DOCX-отчёты — в ОДИН клик (мемоизация по сигнатуре расчёта/набора); (#5) на «Расчёте» xls и отчёт рядом; (#6) «Кластеры этажности» свёрнут по умолчанию; (#8) в «Балансе территории» колонки «Требуется»/«В балансе» — видно компоненты в режиме «только потребность» (#7 — это норматив плотности 450 чел/га, уже подписано). | ✅ |
| **v0.10.7** | `_identify_limiting_factor` теперь распознаёт **норматив плотности 450 чел/га** как ограничивающий фактор: когда население упёрлось в потолок плотности, а территория осталась (большой «резерв»), сообщает об этом явно (раньше показывал «ЗУ жилой застройки …%», что вводило в заблуждение). Резерв при этом — высвобожденная плотностью земля (уходит в озеленение/двор). Тесты 371 → 373. | ✅ |
| **v0.10.6** | Тесты-гарантии корректности: (1) кластеры с равной этажностью == одиночная этажность бит-в-бит; (2) «пик площади на средних этажах» зафиксирован как следствие ступени ЗНОП ПЗЗ (КИТ≈1.6: ЗНОП 0→4 м²/чел) — на пике ЗНОП=0, выше площадь меньше; (3) без ЗНОП площадь монотонно растёт. Подтверждает: контринтуитивный оптимум этажности — это норматив, не баг. Тесты 368 → 371. | ✅ |
| **v0.10.3 (UX)** | По замечаниям: (1) редактор зон — единый чекбокс «🏗 Разная этажность по зонам» (без вложенного expander и двойного действия), площадь квартала = Σ зон, убраны колонки мин/макс этажности; (2) диапазон этажности зон для подбора перенесён в «Оптимизация → Настройки подбора» (`ParetoConstraints.cluster_floors_range`) — если не задан, зоны фиксированы как в базе; (3) убрана растяжка кнопок на всю ширину (18 кнопок/download → натуральная ширина), CSS «кнопочного» вида; (4) приглушены яркие сине-зелёные заливки колонок «Параметры» → нейтральные карточки с тонким акцентом. Точка отката: commit `b326a62` (v0.10.2). | ✅ |
| **v0.10.2** | **Деловые DOCX-отчёты для Заказчика (альбомные)**: `build_variant_report(name, tep, path)` — подробный отчёт по ОДНОМУ варианту (титул, общие ТЭП-сетка, баланс территории таблица+barh-диаграмма, жильё/соц/парковки, кластеры этажности, экономика cost↔revenue таблица+стек-диаграмма); `build_comparison_report(scenarios, path)` — сводная таблица + диаграмма сравнения + рекомендация. Фирменный стиль (синяя шапка таблиц `w:shd`, зебра, отступы ячеек, колонтитул с PAGE, заголовки секций с подчёркиванием). Кнопки «📄 Сформировать отчёт по варианту» (Расчёт) и «Сформировать отчёт» (Сравнение). `build_report`=алиас на comparison. Тесты 363 → 366. | ✅ |
| v0.11 | DEAP / NSGA-II — Парето-фронт (площадь × резерв × прибыль). | ⏳ |
| v0.12+ | Новые региональные профили; CLI. | — |

**Снято с карты:**
- ~~Расширение соцобъектов специальной логикой (поликлиники, ФОК, культура)~~ — покрывается универсальным `CustomObject` с ВРИ-кодом (с v0.5.5).

⏳ — запланировано; ✅ — реализовано.

## Принципы разработки

- **Цифры — только в YAML.** Никаких magic numbers в коде.
- **Расчёты — чистые функции.** На вход числа и нормативы, на выход — `TEPField`. Никаких побочных эффектов.
- **Источник у каждого норматива.** Без `source` параметр не принимается.
- **Pydantic для всего, что приходит извне.**
- **Регион переопределяет, не дублирует.**
- **Один движок — все режимы.** Все режимы дёргают один `compute_tep_for_kit`.
- **Расширяемость через типы, а не через ветвления.** Новый вид норматива → новый `type`. Новый ВРИ → новый ключ. Код не разрастается.

## Что НЕ делать

- Не зашивать численные нормативы в код.
- Не добавлять геометрию в v0.1–v1.0.
- Не подключать UI до v2.x. Сейчас — Python API + Jupyter.
- Не обобщать заранее. Усложнения добавляются, когда появляется второй конкретный случай, требующий обобщения.
- Не трогать docx-источники в `info/` из кода.
