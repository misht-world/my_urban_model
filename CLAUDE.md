# my_urban_model

Математическая модель застройки территории для оценки вариантов проектных решений и расчёта ТЭП. Замена связки Grasshopper + Galapagos для математической части задачи (без геометрии на старте).

## Контекст

- Автор: Михаил, фоновое образование в градостроительстве (опыт работы с GIS в СПб).
- Работает независимо, проект для собственного использования.
- Раньше использовал Grasshopper + Galapagos. Уходит от этого в сторону Python ради гибкости, версионирования через git и более мощных оптимизаторов.
- Проект живёт в `D:\Github\my_urban_model\`. Зеркало: https://github.com/misht-world/my_urban_model
- Источник истины по задаче — `info/ТЗ_обратный_расчет_ТЭП.docx`. Параметры — `info/Параметры застройки территории.docx`. ВРИ-справочник — `dict/dict_vri.yaml`.

## Цель

**Обратный расчёт ТЭП.** Пользователь задаёт площадь квартала, регион (СПб), ограничения и состав учитываемых объектов; программа подбирает максимально допустимый КИТ, при котором выполняется баланс территории и нормативные требования, и возвращает связанные ТЭП.

Прямой расчёт (`compute_tep_for_kit(kit, ...)`) — внутренняя функция; обёрнута в публичный режим `verify_kit` для проверки заданного КИТ.

## Архитектура (5 слоёв)

```
configs/        — YAML: нормативы по ВРИ-кодам. Цифры живут только тут.
src/urban_model/
  normatives/   — Pydantic-схема, loader (russia↔spb наследование), resolver.
  models/       — Site, Housing, Social, Parking, DecisionVariables, TEPField, TEPResult, Scenario.
  calculations/ — чистые функции без побочных эффектов. Ядро.
  core/         — forward (compute_tep_for_kit) и inverse (solve_max_kit, бисекция).
  modes/        — публичные точки входа: inverse, verify, compare.
  export/       — TEPResult → DataFrame / xlsx с цветовой маркировкой статусов.
```

Принцип: расчётное ядро не знает ничего про режимы работы. Один движок — все режимы.

## Стек

| Компонент | Технология |
|---|---|
| Язык | Python 3.11+ |
| Менеджер зависимостей | uv |
| Валидация | Pydantic v2 |
| Конфиги | PyYAML |
| Таблицы | Pandas |
| xlsx | openpyxl |
| Тесты | pytest |
| Демо | Jupyter |
| Оптимизация одно-крит. | Optuna (с v0.5) |
| Оптимизация мульти-крит. | DEAP / NSGA-II (с v0.6) |

Геометрия (Shapely/GeoPandas) и UI — за рамками v0.1–v1.0.

## Нормативы — структура

Двухуровневая система YAML: `configs/russia.yaml` (база, СП 42.13330.2016) + `configs/spb.yaml` (`parent: russia`). Регион переопределяет только то, что отличается; недостающее наследуется от базы.

**Ось организации — ВРИ-код** (Приказ N П-0412). Озеленение и парковки в ПЗЗ СПб структурированы именно так. Семантические алиасы (`kindergarten = "3.5.1"`, `housing = "2.6"`) — поверх, для удобства.

Каждый параметр имеет явный `type`:

- `scalar` — простое число (мест/1000 = 61, КИТ_max = 2.5).
- `piecewise` — кусочно-постоянная функция от аргумента (площадь участка ДОУ от вместимости; ЗНОП от КИТ).
- `conditional` — зависит от категориальной переменной (мин/макс вместимость ДОО от building_type).

Каждый параметр обязательно содержит `source` — ссылку на нормативный документ (СП, НГП, ПЗЗ, РМД, письмо). Без источника параметр не принимается — он юридически невалиден.

В коде универсальный интерфейс:

```python
norms = load_normatives(profile="spb")
plot = norms.resolve("social_objects.kindergarten.plot_area_per_place",
                     capacity=180)  # → 40 (попадает в "до .inf" ступень)
kit_max = norms.resolve("kit_limits", planning_doc="yes")  # → 2.5
```

## TEPResult — структура

Каждое поле результата — это `TEPField`, а не голое число:

```python
TEPField(
    value: float,            # принятое к расчёту значение
    normative: float | None, # нормативное (без округлений и ручных правок)
    user_value: float | None,# пользовательское (если задано вручную)
    status: Status,          # ok / warning / error / manual / no_data
    source: str | None,      # ссылка на документ
    formula: str | None,     # текст вида "население × 61 / 1000"
)
```

Это закрывает требования ТЗ: историю расчёта (§19.1), статусы показателей (§19.3), видимость ручных правок (§12), ограничивающий фактор в `solve_max_kit`.

## Парковочные сценарии

`ParkingConfig.mode` — три режима, дёргают одну и ту же функцию `compute_parking_breakdown`:

- `min_open` — открытых ровно норматив-минимум (12.5%), остальное в подземных. По умолчанию.
- `all_open` — 100% открытые. Максимальная нагрузка на поверхность квартала.
- `custom` — пользователь задаёт три доли (open + multilevel + underground = 1.0).

Многоуровневые наземные паркинги дробятся по нормативу `multilevel_capacity_max` (300 м/м для СПб). Пятно вычисляется по piecewise `multilevel_area_per_place(levels)` и **попадает в баланс квартала отдельным компонентом**. Подземные не занимают поверхностной площади.

## Публичный API

```python
from urban_model import (
    solve_max_kit,
    solve_max_kit_with_reserve,
    solve_max_kit_with_znop,
    verify_kit,
    compare_scenarios,
    run_scenarios,
)
from urban_model.models import (
    Site, CalculationOptions, Scenario, ParkingConfig, BuiltInArea,
)
from urban_model.normatives import load_normatives
from urban_model.export import to_xlsx, results_to_dataframe
from urban_model.optimize import SearchSpace, optimize_max_apartments  # v0.5
```

## Оптимизатор (v0.5)

Galapagos-аналог на Optuna. Цель — максимизация площади квартир при выполнении всех ограничений (балансовый surplus ≥ 0, плотность в норме). Декрейшн-переменные: этажность, режим парковок и доли open/multilevel/underground, этажность многоуровневых паркингов, число ДОО, число СОШ, ВПП on/off.

Каждое испытание Optuna строит `CalculationOptions` поверх базового шаблона и вызывает `solve_max_kit` — то есть оптимизатор работает **поверх обратного расчёта**, а не вместо него. Это держит ядро вычислений простым: оптимизация — отдельный слой.

```python
from urban_model.optimize import SearchSpace, optimize_max_apartments

space = SearchSpace(
    floors_range=(8, 25),
    parking_modes=["min_open", "all_open", "custom"],
    parking_open_share_range=(0.1, 0.5),
)
report = optimize_max_apartments(site, base_options, norms, space, n_trials=100)
print(report.best.apartments_area, report.best.params)
for r in report.top_n[:5]:
    print(r.rank, r.kit, r.params)
```

В UI (вкладка «🧬 Оптимизация») — форма выбора варьируемых параметров + слайдер trials + таблица топ-N + кнопки «Добавить в сравнение».

## Определение КИТ (важно!)

Текущая модель использует определение КИТ из ПЗЗ СПб:
```
КИТ = площадь квартир / площадь ЗУ жилой застройки
```
Это значение лежит в `result.kit` и сравнивается с нормативом (≤ 1.4 без ДПТ; ≤ 2.5 с ДПТ). К нему же применяется piecewise-функция ЗНОП.

Внутренняя переменная бисекции — `block_density` = `GFA / S_квартала` — **не путать** с КИТ. Доступна как `result.block_density` для аудита/отладки.

## Дорожная карта (актуализирована 2026-05-10)

| Версия | Содержание | Статус |
|---|---|---|
| **v0.1** | uv-скелет; нормативы (russia+spb) с piecewise/conditional/наследованием; `TEPField/TEPResult`; чистые расчёты ТЭП по ТЗ; `solve_max_kit` через бисекцию; ДОО + СОШ; тесты; demo-notebook. | ✅ |
| **v0.2** | `verify_kit`, `compare_scenarios`/`to_xlsx`, `solve_max_kit_with_reserve`, `solve_max_kit_with_znop`, **ВПП как сущность** (`BuiltInArea`). | ✅ |
| **v0.3** | Парковочные сценарии (open/multilevel/underground); WARNING при СОШ < нормативного минимума; **Streamlit-UI** (sidebar + KPI + expanders + сравнение + xlsx). | ✅ |
| **v0.5.0** | **Optuna-оптимизатор** (`src/urban_model/optimize/`): `SearchSpace` + `optimize_max_apartments`. Вкладка «🧬 Оптимизация» в UI. Override ДОО/СОШ в UI. | ✅ |
| **v0.5.1** | **Контроль норматива 25% озеленения квартала** в `balance.is_feasible` (раньше был справочной величиной). Хотфикс CLI `run.py`. SchoolSpec defaults False/False. | ✅ |
| **v0.5.2** | `Scenario.mode` расширена до 4 режимов (with_reserve, with_znop). Optuna exception reporting + ВПП-trap warning. | ✅ |
| **v0.5.3** | UI текст: ДПТ, ЗНОП, ДОО, парковки, СОШ message — нормальные русские лейблы. | ✅ |
| **v0.5.4** | **Переопределение КИТ** на ПЗЗ-формулу `apartments_area / housing_lot`. ZNOP теперь по правильному КИТ. Внутренний `block_density` (GFA/site) — отдельное поле в TEPResult. | ✅ |
| **v0.5.5** | `CustomObject` — список произвольных объектов с ВРИ-кодом (офис/ФОК/поликлиника). Каждый занимает свой ЗУ, требует парковок и даёт озеленение по ВРИ. | ✅ |
| **v0.5.6** | Равномерное распределение мест в ДОО/СОШ (`distribute_places_evenly`). Фикс ParkingConfig validation в Optuna. Оверрайд долей проездов в UI. Отдельная вкладка «📦 Объекты» с табличным редактором. Улучшена ясность UI оптимизатора парковок. | ✅ |
| **v0.5.7** | Аудит-фиксы: `__init__` версия и `__all__`, валидация capacity_min/max, magic numbers убраны из formula-строк, дорожная карта актуализирована, документировано ограничение CustomObject parking. | ✅ |
| **v0.6.4** | UX-батч (9 пунктов): lock-icon, ДПТ выше этажности, крупные вкладки, KPI-ряд2 (ДОО/СОШ/паркинг/ЗНОП), CSS-уплотнение, ЗНОП-лейблы, ВПП в блок жилья, пресет 50/50, превью топ-N оптимизации. | ✅ |
| **v0.6.5** | **Встроенно-пристроенный ДОО**: ЗУ = 24 м²/место (ПЗЗ СПб); здание ДОО вычитается из жилой GFA. Фикс счётчика вкладки «Сравнение». «N объектов по M мест» в KPI. CSS-вкладки — крупнее и выразительнее. capacity_min/max для built_in ДОО: 120/350. **СОШ-нормативы по данным КС** (параллели II–IX): rounding=25 (СП Градостроительство), capacity_max=2475, новый список allowed_capacities, обновлены plot_area/building_area piecewise. WARNING если вместимость СОШ не из списка [550/825/1100/1375/1650/1925/2200/2475] — с указанием ближайших. Аудит-фиксы (17 пунктов): magic numbers убраны в YAML, `type: list` в схеме нормативов, общий хелпер `allowed_capacities.build_warnings`, источники через `norms.source_of()`. | ✅ |
| **v0.6.6** | **«Только потребность»** для ДОО и СОШ (`KindergartenSpec.only_demand` / `SchoolSpec.only_demand`): объект учитывается для подсчёта мест, но ЗУ/здание не входят в баланс квартала и не вычитают жилую GFA. **Piecewise `driveways.housing_lot_share` по этажности**: ≤4 эт. → 100% от застройки, >4 → 120%. | ✅ |
| **v0.6.7** | **Редизайн вкладки «Параметры»**: двухколоночная компоновка. Слева — «Общие сведения о территории» (площадь, ДПТ, этажность, expander «Проезды на ЗУ») и список чекбоксов «Учитывать в расчёте». Справа — плитки настроек для каждого включённого компонента. Внутренние чекбоксы «Учитывать ДОО/СОШ/ЗНОП/ВПП» убраны из плиток (управляются снаружи). **Новые флаги** `include_parking`, `include_znop`, `include_intra_driveways` в `CalculationOptions`: при `False` компонент = 0 в балансе/ЗУ/озеленении. | ✅ |
| **v0.6.8** | **Плоскостные спортивные сооружения (ВРИ 5.1.3)**: 1000 м²/1000 чел + 40% озеленения (ПЗЗ); до 49% озеленения замещается самой спортплощадкой (п. 1.9.4 ПЗЗ). Новый модуль `calculations/sport.py`, секция `sport_facilities` в `spb.yaml`, `SportFacilitiesSpec` + `include_sport_facilities` (по умолчанию True). Компонент в balance.components, плитка в UI с чекбоксом «только потребность». Пример: 1000 чел → ЗУ = 1204 м². | ✅ |
| **v0.6.9** | **Override площади спортсооружений** (`SportFacilitiesSpec.area_override_m2`): задать вручную в м² или га; озеленение и ЗУ пересчитываются от заданной площади. **UI-правки**: СОШ-чекбоксы (бассейн/спорт-ядро) вертикально, ВПП-radio без «(рассчитать)». | ✅ |
| **v0.7.0** | **Парковки ДОО и СОШ** по формуле ПЗЗ СПб: `max(2, ceil(workers/5) + ceil(students/100))` на каждый объект. Работники по вместимости — piecewise из данных КС (`workers_per_capacity` в `spb.yaml`). Норматив парковки — `parking.social_objects`. **Архитектурно**: парковки соцобъектов — отдельные открытые на собственном ЗУ соцобъекта; в общий пул жилищных НЕ вливаются. Площадь = `places × 20.75 м²` входит в баланс как компонент `social_parking_plot` и вычитается из знаменателя 25%-озеленения. При `only_demand` / `include_*=False` — не учитываются. | ✅ |
| **v0.7.1** | **Список ВПП** с 5 вариантами размещения: full_floor / half_floor / min_only / min_plus / custom_only. Обязательные ВПП по НГП СПб: 4.4 торговля (460.10 м²/1000), 4.6 общепит (105.20 мест × 6 м²), 3.3 быт.обсл. (19 раб.мест × 20 м²), 3.4.1 поликлиника (26.33 посещ × 8 м²), 3.5.1 школа искусств (7.20 мест × 15 м²). Новый модуль `calculations/vpp.py` с `compute_mandatory_areas` + `build_built_ins`. `built_in_list: list[BuiltInArea]` в `CalculationOptions`. UI: радио на 5 режимов с условными полями custom 4.4/4.6 + превью обязательных площадей. Двухпроходный расчёт в `state.run_calculation` (footprint → построение списка → расчёт). Парковки ВПП — **единый средний коэф** 1.56 м/м на 100 м² = 64 м²/м.м (вместо детальной формулы по каждому ВРИ; упрощение по запросу). | ✅ |
| **v0.7.2** | **UX-батч** по результатам тестов: цветовая дифференциация левой/правой колонок «Параметры» (синий/зелёный фон), убраны конкретные цифры из подсказок проездов, убрана длинная caption спортсооружений. **Основные показатели** на вкладке «Расчёт» — в едином блоке с кнопкой копирования сводки в текст. **Плотность** теперь по формуле СП 42.13330 (S_квартир/20 м²/чел / S_квартала). Статусы TEPField на русском (норм./внимание/ошибка/вручную/нет данных). Формулы основных полей переписаны на читаемый русский («Общая площадь зданий (GFA)»). «Добавить в сравнение» переставлено под KPI. Снят отдельный пункт «сверка parking.vpp» — заменён единым средним коэф в v0.7.1.1. | ✅ |
| **v0.7.3** | **UX-батч-2**: усилена цветовая дифференциация колонок «Параметры» (border-left 4px). «Добавить в сравнение» теперь ВНУТРИ блока «Основные показатели». Кнопка копирования сводки — JS-clipboard в буфер ОС одной кнопкой + inline-сообщение «✓ Скопировано». Статус «норм.» → «ok». **Редизайн вкладки «Оптимизация»**: 2-колоночная компоновка (галочки слева, плитки настроек справа), убран expander «Что варьировать», убрана отсылка к Galapagos, парковки/ВПП — галочки вместо selectbox, **добавлен ЗНОП как варьируемый параметр** (`SearchSpace.znop_per_person_choices`), trials/seed скрыты (фиксировано 2000 trials ≈ 20с). | ✅ |
| **v0.7.4** | **Фикс ЗНОП-КИТ связи**: при `znop_per_person_override` теперь применяется обратная piecewise по ПЗЗ — `effective_kit_max = min(kit_limits, znop.kit_cap_for_znop(z_pp))`. ЗНОП=0 → КИТ ≤ 1.59; ЗНОП=3 → ≤ 1.79; ЗНОП=4 → ≤ 1.99; ЗНОП=6 → ≤ 2.50. Раньше override обходил норматив, и оптимизатор находил КИТ=2.5 даже при ЗНОП=3, что противоречит ПЗЗ. Новая функция `znop.kit_cap_for_znop()`. | ✅ |
| v0.8 | **Экономика**: себестоимость, выручка, прибыль (по м² квартир + соц + нежилое). | ⏳ |
| v0.9 | **Документ-отчёт (DOCX)**: сравнение сценариев из session, графики (matplotlib), рекомендация лучшего варианта по экономике + балансу. `python-docx`, нейтральный технический стиль. | ⏳ |
| v0.10 | DEAP / NSGA-II — Парето-фронт (площадь × резерв × прибыль). | ⏳ |
| v0.11+ | Новые региональные профили; CLI. | — |

**Снято с карты:**
- ~~Расширение соцобъектов специальной логикой (поликлиники, ФОК, культура)~~ — покрывается универсальным `CustomObject` с ВРИ-кодом (с v0.5.5).

⏳ — запланировано; ✅ — реализовано.

## Принципы разработки

- **Цифры — только в YAML.** Никаких magic numbers в коде.
- **Расчёты — чистые функции.** На вход числа и нормативы, на выход — `TEPField`. Никаких побочных эффектов.
- **Источник у каждого норматива.** Без `source` параметр не принимается.
- **Pydantic для всего, что приходит извне.**
- **Регион переопределяет, не дублирует.**
- **Один движок — все режимы.** Все режимы дёргают один `compute_tep_for_kit`.
- **Расширяемость через типы, а не через ветвления.** Новый вид норматива → новый `type`. Новый ВРИ → новый ключ. Код не разрастается.

## Что НЕ делать

- Не зашивать численные нормативы в код.
- Не добавлять геометрию в v0.1–v1.0.
- Не подключать UI до v2.x. Сейчас — Python API + Jupyter.
- Не обобщать заранее. Усложнения добавляются, когда появляется второй конкретный случай, требующий обобщения.
- Не трогать docx-источники в `info/` из кода.
